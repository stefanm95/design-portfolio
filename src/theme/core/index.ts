import { colors } from "./colors";
import { dividersGradients } from "./dividersGradients";
import { motionPresets } from "./motion";
import { opacity, opacityClass } from "./opacity";
import { rgba } from "./rgba";
import { textures } from "./textures";
import { ui } from "./ui";

export {
  colors,
  dividersGradients,
  motionPresets,
  opacity,
  opacityClass,
  rgba,
  textures,
  ui,
};

export const core = {
  colors,
  dividersGradients,
  motionPresets,
  opacity,
  opacityClass,
  rgba,
  textures,
  ui,
};

# App progress and capa
sections consume a normalized runtime contract

That means later you can:

swap layout systems
make profile-aware layouts
create cinematic/editorial variants
generate runtime adaptive compositions
orchestrate layouts semantically

without rewriting section components.
